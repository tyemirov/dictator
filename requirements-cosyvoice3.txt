# CosyVoice 3 runtime extras for the GPU image.
#
# Keep this separate from requirements.txt so the base Python app environment
# stays lean outside the GPU container path.
#
# The main CosyVoice models still run on GPU through Torch. The ONNX frontend
# pieces are intentionally kept on CPU because the upstream GPU ONNX wheels do
# not line up cleanly with the Torch 2.8 / CUDA 12.8 stack used by this image.
# CosyVoice still imports pyworld -> pkg_resources, so keep a setuptools line
# that still ships that module.

setuptools<81
numpy==1.26.4
tqdm==4.67.1
huggingface-hub==0.34.3
HyperPyYAML==1.2.3
hydra-core==1.3.2
modelscope==1.20.0
wetext==0.0.4
conformer==0.3.2
x-transformers==2.11.24
pyworld==0.3.4
tiktoken==0.11.0
openai-whisper
onnxruntime==1.18.0
diffusers==0.29.0
gdown==5.1.0
wget==3.2
pyarrow==18.1.0
inflect==7.5.0
